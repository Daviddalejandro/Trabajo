"""Motor de matching (SPEC §8): blocking, scoring por atributo con pesos versionados en
MATCH_RULE, umbrales de decisión y evidencia por comparación (score_detail). Interfaz
`MatchingEngine` desacoplada: producción la sustituye por Splink para > 10M registros."""
