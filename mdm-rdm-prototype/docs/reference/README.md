# Referencias de solo lectura

Colocar aquí, sin modificar:

- `diccionario_party.md` — diccionario maestro de la Jefatura (41 tablas).
- `mdm_party_diagram.mermaid` y `mdm_party_layers_diagram.mermaid` — diagramas ER y por capas.
- `DomainDataModel.xml` — modelo lógico de referencia (IBM InfoSphere).

Sirven para resolver dudas de alcance. El inventario del prototipo es el de la
especificación `SPEC_PROTOTIPO_MDM_RDM_PARTY.md` (§5) y no se amplía desde aquí.
